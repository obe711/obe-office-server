const deviceTypes = {
  PORTAL: 'PORT',
  BLASTER: 'BLAST',
  VEST: 'VEST',
};

const deviceEnum = [deviceTypes.PORTAL, deviceTypes.BLASTER, deviceTypes.VEST];

module.exports = {
  deviceTypes,
  deviceEnum
};